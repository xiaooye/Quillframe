{
  "name": "@quillframe/studio-app",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "vite",
    "quality": "node scripts/product-language-quality.mjs && node scripts/uiux-parity-quality.mjs && node scripts/hardening-quality.mjs && node scripts/authoring-boundary-quality.mjs",
    "lint": "pnpm quality",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "browser:smoke": "node scripts/browser-smoke.mjs",
    "build:assets": "pnpm lint && pnpm typecheck && pnpm test && vite build",
    "build:finalize": "node scripts/write-offline-shell.mjs && node scripts/write-footprint.mjs",
    "build": "pnpm build:assets && pnpm build:finalize",
    "ci:studio": "pnpm build:assets && pnpm browser:smoke && pnpm build:finalize",
    "preview": "vite preview"
  },
  "dependencies": {
    "@solidjs/router": "1.0.0",
    "solid-js": "1.9.14"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.2.1",
    "vite-plugin-solid": "2.11.14"
  }
}
