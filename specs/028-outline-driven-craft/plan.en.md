# Implementation and verification

1. Freeze the researched source versions and current production baseline.
2. Add the bilingual positive-method library and separate diagnostic examples; package the data without duplicating source trees.
3. Freeze opt-in method snapshots in execution requests; extend the existing scene projection with model-owned selection and strictly whitelisted Writer materialization.
4. Cover baseline compatibility, mixed selection, absent/stale plans, invalid references, request replay, revision inheritance, private-input isolation and installed-package loading.
5. Prepare six held-out scene pairs and a bounded anonymous human-review workflow. Verify current capability and cumulative budget before any live call.
6. Run deterministic regression and documentation checks; record actual results and unresolved quality evidence. Leave the candidate off by default until authorized promotion.
