# 本地生产链路验证

状态：真实 CLI 模型通路与上下文绑定回归已验证；用户已授权将新的语义运行纳入累计 96 次上限。本记录区分功能实现、确定性回归、浏览器观察与真实模型结果，不代表已有章节完成、作者采纳或整本导出成功。

## 环境与范围

将 Windows 实际工作区源码和 Studio 构建产物复制到隔离的 Ubuntu WSL 工作区。Python 与 SQLite 由普通 Linux 用户运行，数据位于原生文件系统。源码 SHA-256 清单和详细日志保存在框架版本控制之外；快照同时记录基础 Git 提交与是否存在未提交改动，不能只用提交号替代文件级证据。

原创验收项目位于框架仓库之外，正文、计划、模型请求包和调用账本均留在项目或本地证据存储中。本轮不安装第三方技能，不部署云端，普通 CI 不调用模型。

## 确定性检查

| 检查 | 最近完成的结果 |
| --- | --- |
| 最近完成的冻结 Linux Core 回归快照 | 667 项通过，零跳过 |
| Studio 测试与完整构建，包含运行选择、有效租约保护和语义拒绝显示 | 108 项通过；类型、质量、Vite 和离线构建通过 |
| 云端边界回归与 TypeScript | 90 项通过；类型检查通过；未部署 |
| 文档质量检查 | 零错误，21 项已有警告 |

语义路由检查及全部 30 项注册契约的引用完整性检查也已通过。现有资格自测的 20 项检查通过，覆盖相互矛盾的判定与阻断问题。这些结果不是模型写作质量评分。

## 实际浏览器观察

已在真实浏览器打开原生项目，保存并重新加载全书和章节计划。结算前，Story 中没有已采纳事实，读者期待账本为空。Learning 没有已启用偏好，模型读者反馈明确标为参考意见。Review 没有放行稿件，出版页不能选入未结算章节。

从 Runtime 选择真实运行后，网页打开其注册章节；刷新显示 14 次已确认响应和保留的语义失败记录，没有自动恢复任务或暴露原始草稿。在桌面窗口下，正文与 AI 面板可正常并排阅读；空间不足时，章节和版本侧栏移至编辑区下方。

场景交接修复后，网页保留第二次运行的 13 项已确认结果，明确显示未通过的机制，并禁止恢复该被拒绝的运行；状态刷新仍可使用。已在新的冻结源码上注册并启动第三次运行，沿用原有计划和作者请求。在另一个网页重新打开这次活动运行，即使该页没有发起执行请求，也会显示真实租约并禁用“恢复”；选择和刷新没有触发重复执行。宿主派发失败后，网页显示 `semantic_pending`、保留的错误和无活动执行器，Review 仍为零候选。

刷新第六次运行后，网页显示八次派发与确认、故事与正典预检拒绝，没有活动执行器或待确认调用。页面明确要求处理阻断后注册新运行，没有把调用确认显示成稿件放行。

## 真实 GPT 证据

首次运行调用了 14 个分别新建的 GPT worker，14 份输出均完成精确哈希确认，没有模型重试、输出修改或传输超时。人物行动校验拒绝了第十四份响应，尚无正文放行、作者采纳或结算。

失败暴露了准备阶段的契约缺口：证据放在非标准容器内，虽然模型能够看见，但引用授权只认可注册契约规定的容器；另有一条行动重复引用同一证据。修复后明确使用标准容器，并提供可引用标识清单，原有引用校验和故事时间过滤未放宽；原始失败响应和调用账本保持不变。

关联回归覆盖精确状态枚举、叙事状态的类型化替换字段、只读来源元数据、读者期待更新约束和自检判定一致性。最终观察被拒绝时，运行现在会记录安全且去重的失败事件，停在 `semantic_pending`；恢复保留已确认输出，不增加调用。

第二次运行在真实网页注册并执行，使用修复后冻结的源码快照。13 份响应全部确认，人物阶段通过；场景推演因起始条件未具体化、后续动机缺乏支撑而返回阻断修复项，Core 在 Writer 和候选稿创建之前停于 `failed_gate`。场景输出本身没有明确的通过或失败字段，关卡因非空修复项而拒绝；普通的未解戏剧压力本身不会导致拒绝。

只读诊断发现两处新的交接缺口：初始人物准备没有清楚区分原创候选细节与已采纳 Canon，场景推演收到的行动判断只有证据 ID，缺少其绑定的人物输入。修复后明确创作细节只能作为提案，并将精确人物输入及其指纹交给内部场景推演，不增加模型阶段；场景拒绝规则、人物知识边界和 Writer 私态隔离均保留。新人物路径、私有证据隔离和拒绝强行实现计划的针对性回归已通过。第二次运行的精确输出、停止日志和未变更源码清单均已保全并校验哈希。

第三次运行确认了九份真实 GPT 响应，包括初始人物准备。首个人物行动请求无法创建新 worker，宿主返回 `collab spawn failed: agent thread limit reached`，没有返回任务标识或模型输出。中转超时后，Core 保留空正文的 `model_failed` 结果，停在 `semantic_pending`。因此日志里的十项已确认调用结果包含这次传输失败，并不是十份模型判断。尚未创建原始草稿或候选稿，修复后的场景交接仍未完成真实模型验收。

三次运行累计 36 个不同 worker，36 份精确输出均已核验哈希。宿主派发失败也计入预算，本轮共使用 37/64 次尝试，剩余 26 次生产尝试及一次预留独立审阅；新建运行不会重置总数。实时账本统计实际派发的语义请求和原生独立审阅任务，不声称掌握宿主未提供的 token 账单或内部推理请求次数。三次停止记录及各自源码清单均已保全。独立审阅仍待执行，必须由新 worker 仅根据精确冻结包完成。宿主返回的任务标识不等于服务商 UUID，也不能证明操作系统级隔离。

随后，用户明确授权提交改动并继续执行。本地服务已重新启动，浏览器仍能看到保存的计划与停止记录。新的章节运行已注册，派发数为零。本轮也已成功新建代码审计子任务；独立审稿仍须另建新 worker，不能复用这些已知晓开发上下文的任务。

本机 Codex CLI 已确认使用 ChatGPT 登录，并支持官方的[非交互执行模式](https://learn.chatgpt.com/docs/non-interactive-mode)。生产中转驱动需要显式启动，保留精确输出字节、真实 CLI 会话事件、用量与累计预算，先保存进程证据再发布响应。其 23 项针对性测试在 Linux 下通过，上述原生文件系统完整回归包含这些测试。该驱动不提供原生独立审稿证明，也不把未暴露的服务商内部重试计为已观察调用。

第四次运行从真实网页使用已提交的中转代码启动。首个 CLI 进程在严格配置装载时退出，没有创建线程或返回模型输出。该派发尝试仍保留并计费，本轮累计 38/64 次，剩余 25 次生产尝试及一次独立审阅。Core 保留传输失败，停于 `semantic_pending`，没有创建候选稿。修复前已归档停止日志、账本前缀和源码清单。

使用完整 CLI 参数和空标准输入的检查复现了完全相同的失败哈希。本机版本不再支持旧的 `tools.view_image` 配置项；`--disable view_image` 仍保留。移除旧项后，配置装载通过，并在创建线程或调用模型之前从空输入检查退出。对应的[版本化 CLI schema](https://github.com/openai/codex/blob/rust-v0.150.0-alpha.12.2/codex-rs/core/config.schema.json) 与[事件定义](https://github.com/openai/codex/blob/rust-v0.150.0-alpha.12.2/codex-rs/exec/src/exec_events.rs) 还揭示了新增的 `cache_write_input_tokens` 用量字段。驱动现已原样保留这一非负整数，不改变输出字节、工具拒绝规则或语义门禁；无效和未知用量字段仍会被拒绝。真实 CLI 模型执行成功仍待验证。

第五次运行取得真实 CLI 线程及一份完整 GPT 响应，保存的最终输出与消息哈希完全一致，也能解析为 JSON；但两条正式执行前的错误事件阻止其提交给 Core。根据同版本源码重建事件后，两条哈希都精确匹配：一条是跳过宿主技能发现的实验功能提示，另一条是所选模型要求的 Code Mode 宿主被禁用。Core 再次保留传输失败并停止，本轮累计 39/64 次，剩余 24 次生产尝试及一次独立审阅。没有提交响应或发布候选稿，也不会重放此失败运行。

后续配置仅关闭已识别的实验功能告知，显式禁用 Code Mode 功能，并继续禁用其宿主。下一次运行选用本机目录中没有强制要求 Code Mode 的 GPT-5.5，不改写模型目录或用户偏好。启动错误仍会被拒绝，但现在会保留其类型和消息哈希，方便诊断。无模型调用的目录读取和 11 项 Windows 中转测试已通过；修复后的完整生产链路仍待执行。

第六次运行使用干净的已提交源码 `970f13a`，通过八个独立新建的 CLI 进程调用 GPT-5.5。八份最终消息均与保存的输出字节完全一致，并被 Core 确认。七次上下文选择完成后，故事与正典预检返回 `fail`：选择器遗漏了已保存的目标章节计划，预检还把来源的非权威标记理解为禁止起草。Core 在创建原始草稿或候选稿前停于 `failed_gate`。修改框架之前，已归档源码清单、账本前缀、精确输出和停止状态。

修复将目标章节的当前计划及其全书计划绑定为必需任务输入，与模型原样保留的相关性选择分开记录；材料资格、阶段隔离和硬预算仍然有效。预检与连续性检查现会收到冻结的目标身份，并区分来源权限和内部语义任务，不改变严格的通过或失败门禁，也不授予正典权威。原始失败仍然保留；确定性回归不能证明真实模型已经理解修复后的交接。

本轮累计 47/64 次尝试：此前 36 次原生工作单元、一次失败的宿主派发，以及十次 CLI 进程启动，其中两次 CLI 尝试未成功提交。剩余 17 次包含预留的独立审稿，不足预期的 22 次生产调用加一次独立审稿。将累计上限提高到 96 次的请求仍待用户批准，提出请求不等于获得授权，账本也不会重置。`970f13a` 上的原生 Core 完整回归已实际完成，660 项全部通过。

后续上下文修复在冻结的 Linux 快照上通过全部 667 项 Core 回归，没有失败、错误或跳过；单独执行的 30 项上下文测试、12 项语义上下文测试和 65 项生产运行测试也通过。上下文自测、源码快照的仓库卫生检查及两项文档检查均通过。Windows 整个工作区的卫生扫描另行读到了已忽略的本地辅助工具文档，报告其缺少双语版本和链接；干净的生产源码快照通过了同一检查，没有把这些辅助文件或下游数据加入框架。绑定方式变化会使旧上下文冻结失效，但不会改写语义概况的来源内容；回退到前一提交也须另建绑定源码的运行，不能重新分类已归档失败。修复后交接的真实语义验收仍待执行。

随后，用户明确批准将累计上限提高到 96 次。不可变的本地授权记录绑定了原有 47 次消耗及账本前缀的精确哈希，没有重置账本；剩余 49 次包含独立审稿。中转驱动的默认上限仍为 64，提高上限必须显式指定。队列之外的独立审稿仍纳入编排器的累计审计。这项授权不代表接受尚未看到的候选稿，也不授权结算、后续章节或云端部署。

## 剩余验收关口

冻结已提交修复，在获准的累计上限内执行新的真实单章生成与独立审阅；实际内部草稿存在时，还要核验其公开可见性。再由作者实际决定是否采纳、结算，在获得相应确认且授权预算充足后，才能逐章推进到三章和十二章。按序导出、真实反馈学习及恢复也必须使用这些实际状态验证。四个开发案例和六个留出案例尚未执行，不宣称已证明人类读者留存或百万字一致性。
