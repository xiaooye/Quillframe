# Local production verification

Status: implementation and regression verified; local model continuation is prepared. This record separates implemented behavior, deterministic regression results, browser observations and actual model outcomes. It does not certify a completed chapter, author acceptance or a successful book export.

## Environment and scope

The actual Windows working tree and built Studio assets are copied into an isolated Ubuntu WSL checkout. Python and SQLite run as an ordinary Linux user against a native filesystem. A SHA-256 source manifest and detailed logs remain outside the tracked framework. Snapshots record the base Git commit and whether uncommitted changes are present; the commit alone does not replace the file-level evidence.

The original acceptance project is outside the framework repository. Its manuscripts, plans, model packets and call ledger stay there or in local evidence storage. No third-party skill is installed, no cloud deployment occurs and normal CI makes no model calls.

## Deterministic checks

| Check | Latest completed result |
| --- | --- |
| Latest completed frozen Linux Core regression snapshot | 660 passed, zero skipped |
| Studio tests and full build, including run selection, active-lease guards and semantic rejection display | 108 passed; typecheck, quality checks, Vite and offline build passed |
| Cloud boundary regression and TypeScript | 90 passed; typecheck passed; no deployment |
| Documentation quality | Zero errors; 21 existing warnings |

The semantic router and reference-integrity checks also passed for all 30 registered contracts. The existing qualification self-test passed 20 checks, including contradictory verdicts and blocking findings. These results are not a model-quality score.

## Observed browser behavior

The real browser opened the native project and saved and reloaded book and chapter plans. Before settlement, Story had no accepted facts and the reader-expectation ledger was empty. Learning had no active preferences; model-reader feedback was labeled advisory. Review had no released candidate and publication excluded unsettled chapters.

Selecting an actual run from Runtime opened its registered chapter. Refresh showed 14 confirmed responses and a retained semantic failure, without resuming the run or exposing raw prose. The editor remained usable beside the AI panel at a desktop viewport; narrow side panels moved below the editor.

After the scene handoff fix, the browser retained the second run's 13 confirmed results, displayed its specific failed mechanism and disabled resuming that rejected run. Status refresh remained available. A fresh third run was registered and started with the original saved plans and author request against the new frozen source. Reopening that active run in another browser page showed the real lease and disabled Resume even though that page had not issued the execution request; selecting and refreshing did not dispatch a duplicate execution. After the host dispatch failure, the browser showed `semantic_pending`, the retained error and no active executor; Review still had zero candidates.

## Actual GPT evidence

The first run dispatched 14 fresh GPT workers. All 14 exact output hashes were confirmed; there were no model retries, output edits or transport timeouts. The character-action validator rejected the fourteenth response. No manuscript was released, accepted or settled.

The failure exposed a producer-contract gap: evidence was visible inside noncanonical containers, while reference authorization recognized only the registered containers. One action also repeated an evidence reference. The repaired producer uses explicit canonical containers and an eligible-reference index without weakening reference or story-time validation. The original failed response and ledger remain unchanged.

Related regressions cover exact stage-status enums, typed narrative replacement fields, read-only source metadata, reader-expectation update constraints and consistent self-audit verdicts. A rejected final observation now records a safe, deduplicated failure and stops in `semantic_pending`; replay retains the confirmed output and makes no new call.

The second run was registered and executed in the real browser against a frozen repaired snapshot. All 13 responses were confirmed and the character stage passed, but scene resolution returned blocking repair routes for unspecified starting conditions and unsupported motivation. Core stopped in `failed_gate` before Writer or candidate creation. The scene output has no explicit pass/fail field: its nonempty repair routes caused the gate rejection. Ordinary unresolved dramatic pressure alone does not cause this gate to fail.

Read-only diagnosis found two further handoff gaps: initial cast preparation did not clearly distinguish original candidate details from accepted Canon, and scene resolution received action judgments with evidence IDs but not their bound character input. The follow-up repair clarifies proposal-only creative detail and supplies the exact character input with its fingerprints to private scene simulation. It adds no model stage and retains the scene rejection rule, character knowledge boundaries and Writer's private-state exclusion. Focused regressions passed for the new-cast path, private evidence isolation and refusal of a planned outcome. The second run's exact outputs, stopped journal and unchanged source manifest have been preserved and hash-verified.

The third run confirmed nine genuine GPT responses, including initial cast preparation. Its first character-action request could not obtain a new worker: the host returned `collab spawn failed: agent thread limit reached`. No task handle or model output was returned for that request. Core retained the empty `model_failed` result after the relay timeout and stopped in `semantic_pending`. Its journal therefore has ten confirmed call results, including this transport failure, not ten model judgments. No raw draft or candidate was created, so the revised scene handoff has not yet completed a real-model acceptance run.

Across all three runs, 36 distinct workers returned 36 exact, hash-verified responses. The failed host dispatch is also charged, bringing the round to 37 of 64 attempts. There are 26 further manager attempts and one reserved independent review left; a new run does not reset that total. The live ledger counts actual dispatched semantic requests and native reviewer invocations, not the host's unavailable token bill or internal inference requests. All three stopped runs and their source manifests are preserved. Independent review remains pending and must use a fresh worker with only the exact frozen packet. A canonical host task handle is not a provider UUID or an operating-system isolation attestation.

The user subsequently authorized committing the changes and continuing execution. The local service was restarted and the browser still showed the saved plans and stopped runs. A new chapter run was registered with zero dispatched calls. New engineering subagents could also be created in this turn; independent review still requires its own fresh worker, not one of these informed coding agents.

The installed Codex CLI reports a ChatGPT login and supports the documented [non-interactive execution mode](https://learn.chatgpt.com/docs/non-interactive-mode). The opt-in manager relay now preserves exact output bytes, actual CLI thread events, usage and the cumulative budget, with process evidence persisted before response publication. Its 23 targeted tests pass under Linux; the full native-filesystem Core run above includes those tests. Command parsing was checked without starting a model. No CLI model call has yet been made. The relay is not native independent-review evidence, and unexposed provider-internal retries are not counted as observed calls.

## Remaining acceptance gates

Freeze the committed source and complete real single-chapter generation and independent review within the remaining budget. Verify public draft hiding when an actual private draft exists, and obtain the author's decision before acceptance or settlement. Continue to three and twelve chapters only with those intervening decisions and sufficient authorized budget. Ordered export, real feedback learning and recovery must be verified against those actual states. Four development cases and six held-out cases remain pending. No human retention or million-word consistency result is claimed.
