# Windows Ultra-Long Production Verification

2026-09-01 · `SYSTEM-IMPROVE`

## Verified on Windows

- `cargo fmt --all -- --check`, workspace `check`, Clippy with `-D warnings`, and `cargo test --workspace --all-targets --locked` pass. Current workspace results are Core 71 passed/1 ignored, host 1, native filesystem/publication 7, and secrets 1. The ignored case is the explicitly triggered thousand-chapter metadata durability profile.
- Book Setup regression covers source fingerprints, character/relationship decision models, pressure-bearing world seeds, five-million-character capacity architecture, fixed ending, complete volume/arc/climax binding, author approval, active book-plan binding, interrupted approval recovery, and rejection of an idempotency conflict before plan mutation.
- The three-chapter Core acceptance runs model-mediated context query/selection, Corpus and preference greenlights, private character simulation from the approved Setup, causal scene resolution, Director Note isolation, scene-by-scene prose with minimum-length enforcement, semantic review/repair, acceptance, settlement, typed-ledger projection, publication, restart continuity, and preference learning. Replaying the same production release, acceptance, or settlement request returns the original receipt without creating a second candidate or settlement.
- The same acceptance corrupts an authoritative character projection, proves strict open rejects it, invokes the explicit local Rust Bridge snapshot restore at expected revision 3, and proves strict reopen succeeds. Restore reports `semantic_inference=false`.
- Tauri manifest formatting, Clippy, and its direct-Core boundary test pass.
- Under Node 24 and pnpm 10.33.0, root `pnpm` quality, typecheck, test, and build pass for the product site, SolidJS Studio, and Cloud contract mirror. Studio tests prove explicit model selection is still checked against the Core catalog and business state goes through the typed Bridge.
- Repository scan finds zero `.py`, `.pyi`, `pyproject.toml`, or `__pycache__` files. CI contains a separate interpreter-residue rejection gate.

## Cross-platform evidence

The main CI and Tauri CI both run Core/Tauri on `windows-latest` and `ubuntu-latest`; Linux desktop dependencies are installed explicitly. This Windows record does not fabricate a local Linux result—the Ubuntu jobs remain the authoritative Linux execution evidence.

## Evidence boundary

No synthetic five-million-character endurance run was performed, and the deterministic mock's independent-review stage is not presented as evidence of a genuinely independent invocation. These green gates prove contracts, transactions, stage isolation, and recovery—not the literary quality of a five-million-character novel. A live sequential literary canary, genuinely independent review invocation, and author judgment remain pending.
