# Implementation plan

1. Preserve the stopped run, exact source, original failed result, and cumulative budget. Confirm that no model worker or executor remains active.
2. Implement the optional Agent request bound and HTTP deadline propagation; separately implement the current relay packet and CLI deadline enforcement. Restrict production's longer profile to the two complete-prose stages.
3. Review timing arithmetic, serialization, evidence binding, late-result handling, source/version boundaries, and unchanged AI judgment authority. Test with deterministic clocks and fake processes or local fixture servers, never real models.
4. Run the complete checks, review and commit the exact tested files, and bind the deployed source to that commit. Record limitations without a success or quality claim.
5. Only within explicit remaining authorization, start a fresh production run. Preserve every attempt, perform fresh independent review after qualification, and deliver only the exact Core-released manuscript.

The implementation does not make a stopped confirmed failure resumable. Additional model authorization must not be inferred from the decision to fix the transport.
