# 工作清单

- [x] 核验超时后的停止状态、原始失败结果、源码与累计消耗。
- [x] 区分时间边界与 AI 叙事判断，界定最小改动。
- [x] 实现并测试显式 Agent 与 HTTP 截止时间，包括时钟回拨后的线上截止时间收窄。
- [x] 实现并测试 relay/CLI 到期、证据、迟到结果拒绝及 packet 发布时只能收窄的截止时间。
- [x] 仅为完整正文阶段绑定较长配置，并测试普通默认值、失败结果保留与迟到拒收。
- [x] 完成独立代码复核，包括时钟变化的交接回归，并如实说明进程内时钟的局限。
- [ ] 完成最终全套检查、本地提交及准确源码绑定。
- [ ] 在明确的剩余授权下完成真实生成、独立审查、Core 释放和聊天交付。

本清单记录提交前的实现检查点；最终验证通过另存的执行回执，将准确受测文件绑定到本地提交。未勾选项不得宣称完成。传输测试不证明已有正文或文学质量改善。
