# 网文章节现场感候选机制 · 验证记录

2026-08-28。第三版候选与单篇审阅协议已完成确定性验证。本文只记录实际完成的工程证据；在作者审过新的完整章节以前，文学质量状态保持 `review`。

## 当前证据

- 第二版登记与双语基础层已经逐字节保存，SHA-256 与修改前文件一致。
- 第二版首批两组均记录为 `both_bad`；映射仍封存，剩余两批未审且不得用于推断。
- 单篇审阅准备、发布绑定、反馈链、退回后快照阻断、私有材料隔离和追加式存储已有确定性测试。
- 本轮没有新增模型调用，默认模式仍为 `baseline`，第三版仍为显式启用候选。

## 确定性结果

- WSL Python 3.14.4：共 137 项相关测试通过，包括 18 项创作资源／运行路由、12 项历史配对制品、7 项单篇连续审阅、14 项正文质量契约和 86 项生产运行测试。
- `python3 scripts/docs_quality.py`：0 个错误，21 条均为既有无关文档的非阻断警告；`python3 scripts/quillframe_docs_quality.py`：0 个错误。
- Python 编译、JSON／YAML 解析和 `git diff --check` 通过。首次组合测试命令漏设测试目录的 `PYTHONPATH`，两个模块发生导入错误；按仓库测试路径重跑后 30 项全部通过，没有断言失败。
- 从当前工作树构建并安装 wheel。安装包能读取登记版本 3，包含第二版登记和双语基础层历史，默认 `baseline` 仍不加载候选卡片。
- 当前登记文件 SHA-256 为 `4168261f627cbfbada263923c55756940d2d89ff6e5ce0019df96bedccaf6495`；第三版中文和英文基础层文件 SHA-256 分别为 `c53d9e405cd95b63629090873f17c68adeedef5832362045678d9ceb39e6b333` 与 `009a2be04ac43b1c5d19656d2e842f4ea14ddfc164c5b2941f4757d57394a7a7`。

这些结果证明版本、冻结、回退、隔离和评审顺序成立，不证明第三版已经有网文感。下一项未完成门禁是：另行运行完整生产链，只交一篇全新完整章节给作者绝对审阅。
