# Implementation and verification plan

1. Bind the two `both_bad` outcomes to the displayed version-2 texts, seal the unreviewed batches and close the pairwise review round.
2. Preserve the version-2 registry and bilingual foundation byte-for-byte before changing current resources.
3. Shift the version-3 candidate from surface restraint toward present business, character tactics, responsive action and chapter-level change; expand diagnostics that remain post-generation only.
4. Add a sequential one-chapter protocol requiring full production execution, one visible chapter, author feedback before continuation and a changed snapshot after return or rejection.
5. Preserve pairwise tooling for historical evidence without using it as the current author-review path.
6. Synchronize versions, packaging, bilingual documentation and deterministic tests. Make no model call and claim no literary pass.
