# Plan 022 · Native Independent Review Runtime

## Scope ruling

Framework changes remain generic. Consumer facts and CH001 acceptance live in
an uncommitted local overlay; no Frostloom remote operation is in scope.
Project design stages remain Project-owned inputs; the production executor
continues to execute `DRAFT` and `REVISE`.

## Task 1 · Frozen packet and native evidence core

Add RED tests, migration/repository, generic invocation receipt, first-class
Codex/Claude providers, cross-transport attempt consumption and terminal
replay, Host Bridge v10 dispatch/local surfaces, generic submit alias, and
transport/provider/assurance readiness evidence.

## Task 2 · Native host adapters and legacy transport repair

Add Codex/Claude reviewer agents and lifecycle/tool-denial hooks; consume exact
packets in Project-free local CLI runners; repair GitHub action paths so they
never rebuild a packet; truthfully identify Copilot and remove unsupported
GitHub Models acceptance claims.

## Task 3 · Mapped Project runtime projection

Add optional manifest support, deterministic preview, transactional CAS apply,
status, migration/storage, stage-bounded context materialization, mapped CLI
routing, and zero-model-call target/manifest preflight.

## Task 4 · Integration contracts and documentation

Update paired docs, registries, machine contracts, rollback notes, cross-
component regressions, and deterministic build evidence. Complete task and
final code review before delivery.

## Task 5 · Local consumer acceptance overlay

Copy required local consumer files only. Complete BOOK/VOL1/UNIT1/CH1 design
and the explicit context manifest; validate the Framework successor; apply the
projection; execute `DRAFT(CH-001)`; use one real native Codex review; and read
the released Review Draft only through `candidate.visible.get`. Preserve
`accepted=false` and `settled=false`.

## Rollback

Before Acceptance/Settlement, restore the prior consumer lock/actions,
rebuild projection from Git, and delete ephemeral runtime state. A Review
Draft remains non-Canon. Any fingerprint mismatch fails closed.
