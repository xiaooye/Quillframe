# AI-native fiction-source implementation plan

2026-08-31 · SYSTEM-IMPROVE · no paid canary is authorized by this plan.

## Phase 1 · Preserve the failure evidence

Reconstruct the failed revision’s exact model-call graph, Writer payload and event timeline. Keep the historical candidate unaccepted, unsettled and unchanged. Record proven, bounded and unknown timings separately.

Visible result: a field-level contamination map and an evidence-based explanation of the wall-clock delay.

## Phase 2 · Version the semantic contracts

Add the Author Voice Sheet compiler, expand Character Enactment and Scene Realization, and make scene projection select the Writer context and Director Note. Version the prior contracts before changing current dispatch.

Visible result: model-owned literary choices with Core-owned source and schema boundaries.

## Phase 3 · Replace Raw Draft rewriting

Remove the event-first full-prose stage from DRAFT and REVISE. Dispatch one Surface Writer from the compact context pack. Keep local edits bounded and hide incumbent prose for fresh realization.

Visible result: rejected prose and planning explanations cannot seed a fresh Writer.

## Phase 4 · Preserve author objectives through review

Send the current objective envelope to candidate self-audit and independent review. Require per-objective evidence and conjunctive hard-objective readiness.

Visible result: generic fluency or completeness cannot override an unmet author request.

## Phase 5 · Add fiction routing and governed voice assets

Persist disabled-by-default, author-confirmed Voice Sheets and eligible positive anchors with rights and fingerprints. Require a fiction-writing route and record the exact model version.

Visible result: Quillframe can say what voice evidence and model capability it used, or honestly say none is active.

## Phase 6 · Fix budget and continuation semantics

Split context, output and cost budgets; preserve valid returned responses; bind general checkpoints; enqueue coordinator wakes on result confirmation; recover wakes without duplicate requests; bind each run to one Framework build.

Visible result: model completion advances the run without manual polling and does not lose paid work.

## Phase 7 · Prove mechanism boundaries

Add deterministic tests for Writer visibility, local versus fresh revision, hard objectives, model/result fingerprints, after-response budgets, restart recovery, duplicate charging and script exclusion. Run focused and full model-free suites plus documentation checks.

Visible result: reproducible engineering evidence, with literary quality still explicitly pending.

## Phase 8 · Prepare, but do not run, the canary

Specify candidate models, source-free same-scene inputs, arm order and swapped blind comparisons. Record that this canary has no author-set token or provider-cost ceiling, and ask for an explicit start command before any live call.

Visible result: an explicit source-free canary dispatch packet rather than an implicit model call.
