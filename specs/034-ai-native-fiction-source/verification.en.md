# AI-native fiction-source verification

2026-08-31 · model-free engineering verification complete · no live or paid fiction call authorized.

## Verified engineering claim

Current Quillframe no longer seeds fresh fiction from a complete rejected or assistant-shaped draft. It carries the current author-objective envelope through the direct Surface Writer, self-audit and independent review, and it preserves valid returned model results across soft-budget crossings and coordinator recovery.

This record verifies code paths, schemas, provenance and recovery behavior. It does not claim that generated Chinese fiction is natural, humorous, free of an “AI voice,” or author-approved.

## Frozen pre-change failure baseline

- Active revision objectives reached the Repair Editor and Surface Writer, but not candidate self-audit or independent review.
- The Surface Writer received the complete incumbent candidate, repair explanation, repeated planning material and a large context payload, so a bounded rewrite preserved systemic voice contamination.
- The first revision attempt returned a complete Writer response that was reclassified as `token_budget_exhausted_after_response`.
- The independent reviewer returned PASS with 0.94 confidence without objective-bound evidence; that judgment remains failure evidence, not literary success.
- Across 11 recorded manager calls, wall time was 918.271 seconds: queue 104.746 seconds, worker/model 408.597 seconds, and response-confirm/resume 404.741 seconds. Per-handoff database transitions observed in the event trail were about 20–170 ms; there is no defensible aggregate database or validation total.
- Request-to-visible wall time was about 7 h 09 m and registration-to-visible time about 4 h 03 m. The largest bucket was non-model orchestration: manual polling/resume, test/spec/snapshot work, hotfix and re-dispatch gaps across three builds. Historical events do not support a more precise five-way allocation, so unknown residual time remains unknown.

## Deterministic evidence completed

The following model-free suites passed on 2026-08-31:

- `tests.test_quillframe_production_runtime`: 96/96 in 445.145 s.
- `tests.test_quillframe_author_revision_source`: 14/14 in 100.657 s.
- `tests/test_quillframe_craft_guidance.py`: 27/27 in 55.057 s.
- checkpoint, Writer context, persistence, Agent runtime, Model runtime, host bridge, fiction audition, Author Voice, prose contract and semantic-context modules: 154/154 in 6.247 s.
- `tests.test_quillframe_ai_native_architecture`: 4/4 in 0.100 s.
- `scripts/docs_quality.py`: 0 errors; 21 pre-existing advisory warnings remain.

A repository-wide discovery run also executed 1,152 tests in 848.260 s. Its pre-fix result was 79 failures, 252 errors and 2 skips, dominated by existing Windows no-follow directory limitations, missing Node and Unix process-group facilities, and retired CLI-surface expectations. Two current-contract fixture mismatches found in that run were fixed and rerun green: recorded qualification 14/14 and style-corpus ablation 18/18. The full discovery run was not repeated and remains structurally platform-blocked, so this record does not represent the repository-wide suite as green.

Together these results verify:

1. one direct Surface Writer and no current Raw Draft prose stage;
2. rejected prose, reviewer analysis and private character deliberation absent from fresh Writer context;
3. present-character, model-selected lore and required-author-preference boundaries;
4. rights, scope, version, fingerprint and author-confirmation binding for Voice Sheets, anchors and accepted prose tails;
5. exact current objectives in Writer, self-audit and independent review;
6. fresh realization for systemic contamination and exact edit windows for isolated defects;
7. conjunctive hard-objective readiness: `not_met` or `uncertain` cannot be averaged into PASS;
8. optional prose telemetry is not imported by production decisions, and current schemas expose no English-count, banned-word, length, ratio, AIGC or aggregate literary-score gate;
9. a valid returned response remains confirmed after soft cost or elapsed-budget crossing, with missing billing reconciled separately;
10. exact node checkpoints, durable wakes and billing receipts prevent duplicate dispatch and charge after restart;
11. model protocol, identity and version fingerprint are captured before dispatch and revalidated on resume;
12. cross-build resume requires a typed migration, reconciled billing, an exact persisted offline-regression receipt and explicit authorization;
13. consumer-project identity is absent from current Framework decision modules and contracts;
14. DRAFT/REVISE decision modules neither generate nor execute one-off quality programs.

## Performance status

- Verified: a ninth ready wake is scheduled while eight model waiters are blocked; no manual polling session is required for progress.
- Verified: an expired pollable lease becomes ready within 30 seconds.
- Verified: returned results are committed before any later budget stop, and restart does not duplicate provider billing.
- Partially instrumented: queue, provider/model, durable-result, billing and wake transitions are recorded. Historical aggregate validation, database and orchestration intervals are not all independently timed.
- Pending end-to-end benchmark: serial wall time ≤ actual model sum + 3 minutes, and parallel wall time ≤ model critical path + 3 minutes. Unit clocks and coordinator tests support the mechanism but do not replace a full instrumented canary benchmark.

## Migration and rollback evidence

Current registration uses production-loop contract v10 and craft guidance v4. Production-loop v9 and craft v3 are preserved with exact hashes for audit and rollback evidence. A clean graph cut is used: no adapter or dual dispatch recreates the retired Raw Draft stage.

Historical runs remain frozen and readable. Project-open may apply only ordered, Core-owned known-prefix schema migrations atomically. Cross-build resume fails closed unless the typed migration and exact regression/billing evidence above are present. Disabling current registration preserves Voice Sheet receipts, checkpoints, model results and review evidence; rollback must not make rejected prose eligible or restore Raw Draft generation.

## Canary gate

Before live fiction calls, the authorization packet must name the exact model and call graph, the anonymous author decision rule, and either the rights of every positive sample or an exact source-free declaration. The executed packet was source-free and had no author-set token or provider-cost ceiling: it used no Author Voice Sheet, prose anchor or accepted prose tail. The user narrowed it to exactly two same-scene Writer calls and no model Reviewer.

The packet is executed and the author decision is recorded: treatment was relatively preferred, but both arms were rejected. Status is `engineering_verified / canary_failed_author_rejection`, never `literary_success`.
