# Confirmed-prefix recovery verification

2026-08-31 · deterministic evidence in progress; live literary evidence remains separate.

## Claim under test

The narrow claim is that a fresh, explicitly authorized run can reference four exact confirmed predecessor stages after a terminal self-audit JSON syntax failure, then execute only the four remaining manager gates. No old call is copied, no reused stage is charged again, and the resulting candidate remains a verifiable future revision source.

This record does not claim literary quality, independent approval, release, acceptance, settlement, or Canon.

## Deterministic evidence recorded

- `test_quillframe_author_revision_source` plus `test_quillframe_semantic_context_contracts`: `46/46` passed.
- `test_quillframe_production_runtime`: `95/95` passed.
- The recovery fixture proves the new journal contains only self-audit, comparison, reader expectations, and narrative calls.
- The same fixture proves the source journal is byte-identical, three reuse receipts declare no current model invocation, a changed prefix creates neither run nor call, and a released recovered candidate can start and execute a later local author revision.
- Production regressions retain same-request durable polling, no automatic retry, cancellation, call-budget, independent-review, and Core-release boundaries.
- The recovery fixture now also forces an unsupported narrative evidence quote at `max_model_calls=4`. It proves the authority-free proposal is omitted, no fifth call is dispatched, the run reaches `awaiting_external`, and an identical resume adds no call.
- `scripts/docs_quality.py`: 0 errors and 21 pre-existing warnings outside this specification; `scripts/quillframe_docs_quality.py`: 0 errors.

## Live evidence recorded so far

- Recovery run: `run_29af7d066b1547c89e8719835f02fb67`.
- Frozen prefix: `sha256:46404076c0f4b5a301c79cf231340d245d27b60e9e78049f68ee62824612dc3b`.
- The new journal contains exactly four confirmed roles: candidate self-audit, repair comparison, reader expectations, and narrative state.
- Every slow wait remained the same pending request. Active-epoch usage moved from `7/512` to `11/512`; no poll was charged.
- The initial finalization correctly refused an unbudgeted optional fifth repair before dispatch, but projected the run as `budget_exhausted`. The narrow deterministic fix is verified; live zero-call finalization and independent review remain pending.

## Evidence still required before live execution

- final source snapshot fingerprint and isolated deployment record;
- focused Model, Agent, relay, journal, and native-runner results;
- documentation quality result;
- active budget-epoch reading immediately before registration.

## Live evidence still required

- one fresh independent-review invocation and exact judgment binding;
- final candidate and revision fingerprints;
- active-epoch usage, historical ledger offset, and remaining budget;
- explicit proof that acceptance and settlement were not performed.

## Completion rule

Engineering completes only after focused tests and documentation checks pass. Live execution completes only after the authorized four-call run and one fresh independent review pass Core and expose an unaccepted Review Draft. Literary acceptance remains the user's decision.
