# 已确认前缀续跑实施计划

2026-08-31 · 先完成 `SYSTEM-IMPROVE`，再执行用户明确授权的 CH001 证据续跑修订。

## 阶段 1 · 冻结续跑边界

读取已经静止的来源 run、严格五调用图、终止语法错误、修订来源、上下文、修订计划、阶段回执、候选指纹、当前预算 Epoch 和用户授权；不修改来源 run。

回滚点：尚未创建新 run，也没有模型调用。

## 阶段 2 · 注册由 Core 管理的引用

只接收四项身份字段。在注册事务内重新推导前缀，确认它绑定同一修订来源，并持久化单独的续跑授权回执。

回滚点：任何身份或证据不同，都在插入新 run 前拒绝。

## 阶段 3 · 复用证据，不复制调用

重新验证当前上下文兼容性，写入新 run 的复用回执与确定性复用检查点，在内存中装载精确候选、读者判断绑定和连续性证据。不得再次消费旧文风包，也不得向新日志插入历史调用。

回滚点：装载失败时，新 run 保持零模型调用。

## 阶段 4 · 保住将来的来源复验

让修订来源与作者修订验证能够用“新 run 原生调用 + 前缀引用”重建逻辑日志；场景与读者压力的因果证据沿已验证的父来源递归回溯。

回滚点：无法成为合法后续修订来源的候选稿不得发布。

## 阶段 5 · 验证确定性边界

测试严格四次剩余调用、引用变化时派发前拒绝、旧日志不变、复用阶段零计费、后续修订、普通语义失败、持久等待和生产回归，并运行文档质量检查。

回滚点：真实生产继续停止。

## 阶段 6 · 执行 CH001

把已验证快照部署到隔离运行时。使用用户的 `confirmed_prefix_recovery` 授权注册一个新 run，主调用上限设为四次；遇到持久等待时只轮询原请求，直到经理图进入外部审稿。

回滚点：不得偷偷创建替代 run，也不得扩大冻结上限。

## 阶段 7 · 全新独立审稿与交付

只凭冻结 packet 启动一次独立审稿，保留精确证据；Core 只释放尚未接受的 Review Draft。当前 Epoch 与历史账本分开核对，不接受、不结算候选稿。

回滚点：独立审稿未完成或失败时保留真实状态，禁止换审稿人刷结果。
