# 已确认前缀续跑验证记录

2026-08-31 · 确定性证据仍在收尾；真实文学证据继续单独管理。

## 被验证的主张

本记录只验证：一次新的、由用户明确授权的 run，可以在旧自审因 JSON 语法错误终止后，引用四道精确的已确认前序关卡，并只执行剩下四道经理关卡。旧调用不会被复制，复用阶段不会重复计费，所得候选稿仍能成为可复验的后续修订来源。

本记录不声称文学质量合格、独立审稿通过、已经发布、已经接受、已经结算或已经进入正典。

## 已记录的确定性证据

- 作者修订与语义上下文组合套件：`46/46` 通过。
- 完整生产运行时套件：`95/95` 通过。
- 续跑夹具证明新日志只包含自审、修订对比、读者期待和叙事状态四次调用。
- 同一夹具证明来源日志逐字节不变；三份复用回执明确声明当前 run 未调用模型；篡改前缀不会创建 run 或调用；恢复稿发布后，还能正常启动并执行下一次本地作者修订。
- 生产回归继续覆盖同请求持久轮询、禁止自动重试、取消、调用预算、独立审稿与 Core 发布边界。
- 续跑夹具现在还会在 `max_model_calls=4` 时强制制造一条不受支持的叙事证据摘录。它证明：无权威提案会被省略，不会派发第五次调用，run 会进入 `awaiting_external`，相同请求再次续接也不会增加调用。
- `scripts/docs_quality.py`：0 项错误、21 项均为本规格之外的既有警告；`scripts/quillframe_docs_quality.py`：0 项错误。

## 已记录的真实执行证据

- 续跑 run：`run_29af7d066b1547c89e8719835f02fb67`。
- 冻结前缀：`sha256:46404076c0f4b5a301c79cf231340d245d27b60e9e78049f68ee62824612dc3b`。
- 新日志恰好包含四个已确认角色：候选稿自审、修订对比、读者期待和叙事状态。
- 每次慢等待都只轮询同一个 pending 请求。活动 Epoch 从 `7/512` 变为 `11/512`，轮询本身没有计费。
- 初次收尾在派发前正确拒绝了无预算的可选第五次修补，但把 run 投影成了 `budget_exhausted`。窄范围确定性修复已经验证；真实零调用收尾和独立审稿仍待完成。

## 真实执行前仍需补齐

- 最终源码快照指纹与隔离部署记录；
- Model、Agent、relay、日志和原生运行器聚焦测试结果；
- 文档质量检查结果；
- 注册前的当前预算 Epoch 读数。

## 仍需真实证据

- 一次全新独立审稿及其精确判断绑定；
- 最终候选稿与修订指纹；
- 当前 Epoch 用量、历史账本偏移与剩余预算；
- 明确证明未执行接受与结算。

## 完成规则

聚焦测试与文档检查全部通过后，工程部分才算完成；四次调用的授权 run 和一次全新独立审稿通过 Core，并暴露尚未接受的 Review Draft 后，真实执行才算完成。文学接受仍由用户决定。
