# Local production evidence · 2026-08-28

This record separates successful transport, valid domain state, manuscript release, and author acceptance. Consumer manuscripts, project identities and private source material remain outside the generic framework.

## First run: stopped before release

The run used local commit `1a4bf27cc4e320902f399cd0dde5c15e06c5cf4c`. All 818 Python tests and the static, documentation, component, bundle and wheel checks passed before execution. The installed source matched all 1,029 verified files. These are local Python 3.14 results, not a remote CI claim.

A fresh native project's first-chapter production made 28 model calls using `gpt-5.5/xhigh`. All 28 had distinct actual worker threads and complete started/finished/submitted records. Original outputs matched the preserved final-message evidence and published responses. Seven calls requested and passed an explicit output schema; the other 21 did not request one. A successful transport record is not a semantic pass.

The full candidate self-audit consumed 39,103 input and 822 output tokens: 39,925 in total, above the previous 32,000-token ceiling and below the tested 64,000-token ceiling.

The subsequent `reader.expectations` result attempted `paid` updates with version zero against an empty `existing_expectations` snapshot. Core correctly rejected the result with `reader_version_mismatch`. The stopped run remains `semantic_pending`, with all 28 transport results confirmed and no active executor. It produced no narrative-state proposal, independent review, visible candidate, author acceptance or settlement. All original results and charges are preserved; repeating the same confirmed result cannot repair it.

## Transport compatibility probe

Local commit `f333ae5142a08989492f84f462ac51bcda2a091d` passed 832 Python tests and all 21 verification groups, including nine exact wheel members; 1,031 source files were bound and verified. A separately charged, original synthetic observation then made exactly one real model call. Its four updates all used `open` and version zero. Original CLI, HTTP and AgentResult output bytes matched, and the original observation validator passed. The nested item branch was exercised; the optional deadline branch was not. The model was not required to return any particular number of updates. This is transport evidence, not a literary or production-release verdict.

## Second production run: frozen-state echo mismatch

A fresh run on the same commit made 12 real model calls with distinct threads. All transport results were confirmed; three calls requested and passed the native output schema. The third character-action result paraphrased the supplied `active_agenda`. Its string shape was valid, but the existing contract binding requires the exact frozen string, so semantic validation rejected it. The run stopped at `semantic_pending`, with no active executor and before raw-draft generation, independent review or release.

Both stopped runs and all original charges remain preserved. The local experiment has used 166 of its authorized 192 attempts; this is an experiment budget, not a default product limit. No historical result is relabeled or replayed under changed source.

## Third production run: complete-prose request timed out

Local commit `e191fd78ba49a258bd334488890ae3459ccc2bd2` passed 835 Python tests and all 21 verification groups. Its 1,031 exact source files were bound to the clean commit. The character identity and agenda echo fields were narrowed to supplied values; their meaning and the action proposal remained AI decisions.

A fresh run then completed eighteen model calls. All six character-action results and the scene resolution passed their explicit output schemas and domain bindings, including the previously failing echo boundary. The nineteenth call, `event_first_raw_draft`, timed out after 150.029 seconds. Its worker produced thread-start and turn-start events but no final message, usage report, output file, or submitted response. Core received HTTP 504 after approximately 170 seconds and retained a `model_failed` result.

The run is inactive at `semantic_pending`. Its nineteen confirmed journal entries contain eighteen completed results and one failure; confirmation must not be described as successful generation. There is no candidate, independent review, acceptance, or settlement. The cumulative experiment now stands at 185 of 192 authorized attempts, with all original spending preserved.

## Follow-up

The [bounded-deadline change](../027-bounded-model-deadlines/spec.en.md) addresses this timing boundary without reducing writing requirements, changing AI judgments, adding retries, or rewriting old results. Its implementation and new live run require separate verification and sufficient explicit remaining authorization. Neither the earlier [prose calibration](evaluation.en.md), the transport probe, nor the stopped runs establish improved literary quality.
