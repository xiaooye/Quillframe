# 本地生产执行记录 · 2026-08-28

本记录区分传输成功、领域状态有效、正文释放与作者接受。小说原文、项目身份和私有来源材料不进入通用框架仓库。

## 第一次运行：释放前停止

本次使用本地提交 `1a4bf27cc4e320902f399cd0dde5c15e06c5cf4c`。执行前，818 项 Python 测试以及静态、文档、组件、分发包和 wheel 检查全部通过；安装源码与 1,029 个已验证文件逐一匹配。这是本地 Python 3.14 的结果，不冒充远端 CI。

原生新项目的首章生产共调用 `gpt-5.5/xhigh` 28 次，实际工作线程各不相同，均保留完整的启动、完成和提交记录。原始输出与保存的最终消息证据、已发布响应一致。其中 7 次请求并通过显式输出格式校验，其余 21 次没有请求该校验；传输成功不代表语义通过。

完整候选自检实际使用 39,103 个输入 token、822 个输出 token，共 39,925，超过旧的 32,000 上限，未超过已测试的 64,000 上限。

随后，`reader.expectations` 对空的 `existing_expectations` 快照提交了版本零的 `paid` 更新。Core 按预期以 `reader_version_mismatch` 拒绝结果。运行停在 `semantic_pending`，28 个传输结果已确认，没有活动执行器；尚未产生叙事状态提案、独立审稿、可见候选、作者接受或结算。原结果和全部消耗保留；重复回放同一份确认结果不能修复问题。

## 输出格式兼容性探针

本地提交 `f333ae5142a08989492f84f462ac51bcda2a091d` 通过 832 项 Python 测试和全部 21 组检查，包括 wheel 内九个成员的逐字节验证；1,031 个源码文件完成绑定与核对。随后，一份原创合成观察材料单独消耗了一次真实模型调用，实际返回四项 `open`、版本零的更新。CLI、HTTP 与 AgentResult 的原始输出字节一致，原领域观察校验通过。本次实际覆盖了嵌套条目分支，没有覆盖可选期限分支；没有要求模型返回特定条目数量。这是传输证据，不是文学判断或生产释放结论。

## 第二次生产：冻结状态回显不一致

同一提交上的新运行实际调用模型 12 次，线程各不相同，所有传输结果已确认；其中三次请求并通过原生输出格式校验。第三个人物行动结果改写了输入中的 `active_agenda`。字符串格式合法，但既有契约绑定要求与冻结原文完全一致，因此语义结果校验拒绝该输出。运行停在 `semantic_pending`，没有活动执行器，尚未进入原稿生成、独立审稿或释放。

两次停止运行及全部原始消耗均保留。本地实验已使用获授权的 192 次中的 166 次；这是本次实验预算，不是产品默认限制。历史结果不会被改判，也不会在替换源码后重放。

## 后续验证

下一项修复只把人物身份与目标回显字段限定为已供应的原值，目标含义和行动提案仍由 AI 判断。真实传输探针已经覆盖字符串枚举；新增的准确值绑定需要确定性回归、重新验证源码，并在剩余预算内开启新生产运行。此前的[文本校准](evaluation.zh-CN.md)、传输探针及两次停止运行，都没有证明文学质量改善。
