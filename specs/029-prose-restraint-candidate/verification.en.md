# Prose-restraint candidate · verification record

2026-08-28. The version-2 resource and rollback boundary are implemented and deterministically verified. The candidate remains opt-in, baseline remains the default, and no prose-quality improvement is claimed.

## Deterministic results

- WSL Python 3.14.4: 129 tests passed — 18 craft-guidance, 11 blind-ablation, 14 prose-quality-contract and 86 production-runtime tests.
- `python3 scripts/docs_quality.py`: 0 errors and 21 non-blocking warnings in unrelated repository documentation. The first run correctly caught the missing version-1 history registration; the manifest was fixed before the passing run.
- `python3 scripts/quillframe_docs_quality.py`: 0 errors.
- JSON parsing, Python compilation and `git diff --check`: passed. Line-ending conversion notices were non-blocking.
- Built and installed a wheel from the working tree. The installed package loaded registry version 2, kept baseline empty by default, and contained the exact version-1 registry and bilingual foundation files.

The initial Windows test command could not import the repository's Unix `fcntl` dependency. The same tests were therefore run under the repository's WSL environment; this was an environment import limitation, not a failed assertion.

## Integrity and evidence limits

The preserved version-1 files match their recorded SHA-256 hashes, and current dispatch tests prove that runtime loading does not read the history directory. No live model call was made for this revision. The reviewer declared no prior sample or mapping exposure; the two first-batch choices are now sealed as blind-eligible observations against the exact presented-text fingerprints. They remain partial version-1 evidence, so a fresh version-2 anonymous comparison is still required before promotion or quality claims.

## Subsequent version-2 result · 2026-08-28

Version 2 later completed 30 bounded live calls over six entirely fresh original tasks, with every request and the version-2 snapshot frozen. After reading only the first batch of two pairs, the author rejected all four texts for literary posing, synthetic voice, missing lived character presence, over-composed dialogue and insufficient Chinese web-fiction atmosphere and plot pressure. This result defeats the version-2 improvement claim. The two remaining batches stay sealed, are not presented and support no inference from unreviewed prose. The default baseline never changed.
