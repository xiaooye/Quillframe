# 正文克制候选机制 · 验证记录

2026-08-28。第二版资源与回滚边界已经实现，并通过确定性验证。候选仍需显式启用，默认基线没有改变，也不声称正文质量已经提升。

## 确定性结果

- WSL Python 3.14.4：共 129 项测试通过，包括 18 项创作指导、11 项匿名消融、14 项正文质量契约和 86 项生产运行测试。
- `python3 scripts/docs_quality.py`：0 个错误，另有 21 条位于无关仓库文档中的非阻断警告。首次运行准确发现第一版历史文档漏登清单；补登后复查通过。
- `python3 scripts/quillframe_docs_quality.py`：0 个错误。
- JSON 解析、Python 编译和 `git diff --check` 均通过；换行符转换提示不构成错误。
- 已从当前工作树构建并安装 wheel。安装包能读取登记版本 2，默认基线仍为空，并包含第一版登记和双语基础层的精确历史文件。

最初的 Windows 测试命令因仓库依赖 Unix 的 `fcntl` 而无法完成导入，随后在仓库使用的 WSL 环境执行同一测试。该记录属于环境导入限制，不是断言失败。

## 完整性与证据边界

第一版三个历史文件与登记的 SHA-256 完全一致；当前分发测试也证明运行时不会读取历史目录。本轮修订没有发起真实模型调用。读者已声明此前没有接触具体样稿或映射，首批两组选择现已与展示正文指纹封存为合格盲读观察。它们仍只是第一版的部分证据；任何推广或质量结论前仍需完成新的第二版匿名对照。

## 后续第二版结果 · 2026-08-28

第二版随后使用六个全新原创任务完成 30 次有界真实调用，全部请求和版本 2 快照均已冻结。作者只阅读第一批两组，便将四篇正文全部判为不满意，理由包括文青、AI 腔、人物不鲜活、对白过度端正，以及缺少中文网文的阅读气氛和剧情张力。该结果否定第二版质量改进主张。剩余两批继续封存，不再展示，也不从未审内容推断结论；默认基线从未改变。
