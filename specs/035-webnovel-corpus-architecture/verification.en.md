# Web-Novel Corpus Architecture Verification

2026-08-31 · current state: contract layer complete; successor runner and literary validation pending.

This file records executed evidence only. Green engineering tests will not be reported as literary uplift, and completed V5 receipts will not be treated as v2 evidence.

## Confirmed boundaries

- The v1 ten-axis protocol and completed V5 run are historical evidence.
- v2 uses independent protocol, schema, fingerprint, and candidate identities.
- Raw story-analysis anchors do not enter Writer or the public atlas.
- Models own literary classification, evidence gaps, and convergence; deterministic code owns boundaries.

## Executed

- v2 mechanism-contract and evidence-request tests: 8 passed.
- Local semantic adaptation and v2 dynamic identity-binding checks: 17 passed; 23 passed when run with the v2 mechanism-contract suite.
- Semantic reference integrity: passed with 48 registered contracts and no catalog/pack mismatch.
- Historical v1 study-runner tests are explicitly skipped; the current Manifest registers no v2 runner and current dispatch rejects v1 contracts.
- The focused set ran 61 tests: 43 passed and 18 historical v1 runner tests were skipped. New modules and semantic-router files compiled, and `git diff --check` passed.

The repository-level semantic-router self-test still fails on pre-existing character-evidence and aggregate-registry checks. This failure is preserved and is not presented as a v2 contract failure.

Neither documentation check is fully green: `docs_quality.py` reports existing unregistered craft-history pages and a home-SVG text-budget issue; `quillframe_docs_quality.py` reports an ordinary English word in an existing Chinese architecture SVG. Those files are outside this Corpus change and were not silently modified.

## Pending

- Focused v2 contract and runner tests.
- Source-free, leakage, holdout, and leave-one-work-out checks.
- Three-arm blind evaluation, order swapping, and manual promotion evidence.
- Re-run relevant deterministic CI after the repository's existing documentation blockers are fixed, then perform a live Chinese web-novel A/B.
