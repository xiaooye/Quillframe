# 语料文风学习 · 实施检查表

2026-08-29。下列已勾选项目代表已持久化研究、已完成的确定性实现，或 `verification.zh-CN.md` 中记录的同标识 V5 核对。未勾选项目需要真实语义运行、人工证据、提交／发布动作或适用权利复核；120 部来源本身不能使任何待办项目自动完成。

当前检查点：V5 仍为 `proposed`，提案哈希为 `sha256:87cebcbc251992ba7b5ed19714117c53d688524c1114734bfaf204e58f6d856b`，120 项均为 `pending`，清单未锁定，未运行语义／文风学习，没有完成回执，也未发布。

## 研究与决策记录

- [x] 阅读仓库治理、`SYSTEM-IMPROVE`、语料政策、自适应学习协议和双语文档规则。
- [x] 持久化行文技法、连载平台指导、可解释文风表示、个性化／适配、记忆／泄漏及长篇评测的一手研究。
- [x] 为每类研究方法记录明确的采用／改造／拒绝结论。
- [x] 保留 `STUDY-GENERAL-QUALITY-REBUILD-V5`；记录此次修正不创建 V6，也不让 V5 作废。
- [x] 记录 120 只是受治理来源批次，绝不是文风学习深度的替代指标。
- [x] 记录身体／外貌描写——包括单独出现的“巨乳”——属于正当 `general` 分析；只有真实上下文建立另行治理的露骨内容区域时才分流。
- [x] 定义感知场景的自适应采样、类型化 `StyleContract`、无来源技法包、Writer 安全公开投影与盲测 A/B 目标方案。

## V5 状态与私有边界

- [x] 修改前重新读取 V5 实时状态，并证明它仍未确认、未执行。
- [x] 冻结可恢复的私有修改前备份、精确提案指纹和依赖清单。
- [x] 只原子刷新同一标识 V5 提案；不得创建 V6，也不得顺带确认、执行或发布。
- [x] 完整作品、来源身份、可逆证据映射、私有评审笔记和来源派生段落留在本地／私有边界，不进入 Git。
- [x] 综合或划分留出集以前，按逻辑作品家族归并不同版本、译本和连载快照。
- [x] 只记录匿名 evidence-scope routes：1 项语言范围信号、3 项偏短／完整性范围信号、2 项重启／合并拼接边界信号；它们不是文学质量 `HOLD`，也不披露来源身份或可还原摘要。
- [x] 将窗口清洁限定为 AI 实际请求片段的拒绝／替代机制；确认前不要求全量窗口暴露。
- [ ] 由用户确认 exact 120 部可用池的分析权利、研究范围、profile 和 proposal fingerprint；membership 勾选不表示逐项文学审查。

## 类型化文风领域

- [x] 实现并冻结十轴 `STYLE_AXES` 的规范顺序。
- [x] 通过 `make_observation` 和 `validate_observation` 实现、测试 `quillframe_style_observation_v1`。
- [x] 通过 `make_craft_candidate` 和 `validate_craft_candidate` 实现、测试 `quillframe_cross_work_craft_candidate_v1`。
- [x] 跨作品编译前要求至少两个不同作品支持和至少一个反例，但不得把数量当作语义证明。
- [x] 实现 `compile_source_free_craft_candidate`、`compile_style_contract` 和 `validate_style_contract`，生成精确 `quillframe_style_contract_v1` 对象。
- [x] 实现 `compile_writer_safe_projection` 和 `validate_writer_projection`，生成精确 `quillframe_writer_style_projection_v1` 对象；不得带证据引用、记录／作品／契约／技法标识或支持／反例数量。
- [x] 为精确 `quillframe_style_leakage_report_v1` 实现 `check_local_leakage` 和 `validate_leakage_report`：本地状态只能为 `pass|blocked`，`release_ready` 恒为假，不返回来源正文，无效输入以类型化错误关闭失败，外部语义检查在本地保持未执行。
- [x] 增加持久化、指纹、模式拒绝、陈旧依赖和失效回执，但不能让新状态耦合正典或生产运行状态。

这些组件具有聚焦的构造、拒绝和持久化测试。最终集成结果另行记录，也不证明语义收敛、文学改善或发布授权。

## 感知场景的学习

- [x] 建立只负责机械边界、定位和指纹的可寻址范围清单，不保存来源正文。
- [x] 移除关键词／标点分值对场景功能和文风类别的决定权：确定性信号只作为检索提示，实际观察到的场景功能、证据范围与不确定性由模型契约返回。
- [x] 为单作品观察与综合提供绑定的语义契约，并让语义结果表达覆盖缺口和局部饱和状态。
- [x] 让模型契约而非顺序队列选择下一份合格作品与场景功能证据请求；合成运行器测试证明可审计的动态激活，同时未触及成员继续保持可用且未分析。
- [x] 允许模型契约基于跨作品证据提出提前收敛，而不要求 120 部全部完成；登记契约与合成运行器测试证明了工程路径，不代表真实 V5 或文学结果。
- [x] 为预算耗尽、权利阻断和矛盾未解保留类型化状态；这些状态不能冒充文学结论。
- [x] 使用有界读取流式规范化所有 TXT，包括低于旧内存阈值的来源；已选窗口只进入同轮临时缓存，只有崩溃或新进程恢复时才按精确指纹重新物化，不再为每次语义调用重复扫描。
- [x] 将精确受治理／原始来源状态与解码全文、上一轮清单和最终片段指纹绑定；采样、重放或物化时任一层漂移都拒绝。
- [x] 网址／域名、HTML／脚本和分发／导航样板按整窗口拒绝并确定性补位；不把普通身材／外貌描写或孤立的“巨乳”误判为污染或成人配置信号。
- [x] 增加对白、动作、内心、说明、过渡、环境、关系、身体／外貌、开篇、中段和收束夹具。
- [x] 每条语义观察绑定协议、来源版本、片段和模型执行来源；拒绝引文、近似复述和来源特有叙事细节。

## 无来源编译与公开投影

- [x] 只编译经过跨作品对照与反例复核后仍成立的条件、操作、效果和失败边界。
- [x] 第一版契约不包含示例；未来如需示例，只能使用与来源无关的合成构想，禁止改写来源场景。
- [ ] 对私有边界执行精确词元／n 元语法、近似字符／MinHash、身份／专名／内容及外部语义泄漏检查。
- [x] 证明公开／Writer 投影只含精确安全模式，并设置 `attribution_mode=source_free` 与 `semantic_leakage_check=required_external`。
- [x] 聚合证据数量只留在内部契约；来源／证据引用只留在私有记录。
- [x] 强制保证泄漏政策、内容区域隔离、回滚绑定和人工明确发布授权全部通过以前无法发布投影；仓库默认信任策略保持未配置，发布继续阻断。

## 运行时隔离与评测

- [x] 增加明确选择才生效的候选模式，不改变既有默认行为。
- [x] 按场景需要选择零到四张卡；根据冻结契约验证选择，并保持当前用户／项目权威优先。
- [x] 安全投影只路由到内部初稿和表层实现 Writer 阶段。
- [x] 增加负向测试，证明盲读者、独立评审、正典／状态消费者和隐藏预期数据都看不到投影与实验身份。
- [x] 冻结与来源无关、按作品家族留出的小说任务，覆盖相关“场景功能 × 文风轴”单元。
- [x] 在相同输入上，构造无指导基线、当前技法 v4 与语料候选之间密封、交换顺序的盲测比较。
- [x] 分别报告内容忠实度、因果运动、目标机制、自然度、可读性、吸引力、多样性、原创性和泄漏；不得按一个加权分数自动提升。
- [ ] 收集独立语义判断和作者／真人明确决策；候选、协议、实验处理或来源依赖实质变化时让旧判断失效。

## 验证、文档与发布

- [x] 运行聚焦单元／集成测试、完整确定性回退测试、模式／指纹夹具、Python 编译、JSON／YAML 解析、文档质量检查和 `git diff --check`。
- [x] 历史流式安全／回退组与大文件 CPU／内存数据已记录为适配器诊断；它们不是 V5 确认、语义收敛、文学质量或发布门，也不要求对 120 部执行全量性能 smoke。
- [x] 验证安装包行为，以及每个预期公开制品的清单／打包文件覆盖。
- [ ] 只有经过精确确认与授权后才能执行真实 V5 语义工作流，并保存运行回执和失败状态。
- [ ] 完成长篇留出盲测文学评估，并记录人工提升／拒绝证据。
- [x] 实现稳定后同步语料政策、自适应学习文档、语义契约目录、登记／运行时文档、发布说明和双语镜像。
- [ ] Git 中只提交无来源、已验证的公开制品；绝不提交本地完整作品。
- [ ] 泄漏检查只是风险控制，不是版权许可；把公开库称为可发布以前，要完成适用的发布复核。
