# 语料文风学习 · 验证记录

2026-08-29 · 总体状态：`engineering_partial_live_evidence_pending` · 来源池证据分流：`evidence_scope_routed` · 语义／文学状态：`not_run` · 公开发布：`blocked`

本记录刻意区分已经完成的研究／规格工作和尚不存在的证据。本目录文件定义目标并保存研究，不证明 Quillframe 已经学会文风，不证明 V5 已经执行，不证明候选改善了写作，也不构成公开制品的法律许可。

## 实际已完成证据

- 起草以前已阅读仓库治理、`SYSTEM-IMPROVE` 协议、语料／自适应学习边界和双语文档规则。
- `research.en.md` 与 `research.zh-CN.md` 中的一手来源已按作者、出版方、会议或官方项目直达页面核对；研究记录明确区分采用、改造与拒绝。
- 双语研究、规格、计划和检查表已经记录同一组固定决策：保留 `STUDY-GENERAL-QUALITY-REBUILD-V5`，不建 V6，不让 V5 作废；120 只代表来源批次；使用感知场景的自适应采样；编译无来源技法包；隔离 Writer 投影；要求盲测 A/B；身体／外貌描写继续属于 `general`，除非真实露骨上下文建立另一内容区域。
- 实现与规格已经和约定的精确接口名、规范十轴顺序、第一版模式名对齐。下方已记录确定性工程证据，但它仍不能替代真实语义运行或文学判断。
- 研究／规格起草过程没有打开或阅读本地小说副本目录下的文件。随后经授权的只读质量审计仅在私有边界内执行；本仓库只记录聚合分流数量。本规格目录没有复制完整来源、引文、近似复述、标题／作者身份、路径、可还原摘要或可逆证据映射。

## 同标识 V5 核对证据

- 本地修改开始以前，已直接从私有 SQLite 账本重新读取 `STUDY-GENERAL-QUALITY-REBUILD-V5`：状态为 `proposed`，120 部作品全部为 `pending`，清单哈希为空，没有失效原因，也没有语义完成依赖。修改前精确提案指纹为 `sha256:87cebcbc251992ba7b5ed19714117c53d688524c1114734bfaf204e58f6d856b`。
- 扫描或刷新以前，已在私有备份目录生成 SQLite 一致性备份。备份文件名为 `corpus-before-style-v5-refresh-20260829-001042.sqlite`，SHA-256 为 `401f075fa9d3875a19e10de8c90b83d6360b9295f88ac817cc571a789521a030`，大小为 856,064 字节。没有把原文或私有定位符复制进 Git。
- 获授权的本地集合扫描完成：共检查 387 个候选文件定位，归并为 382 个逻辑作品；384 个来源版本未变，没有创建新版本，没有让任何研究失效，也没有持久化原文。7 个输入安全拒绝：3 个超过当时的来源大小上限，3 个缺少 EPUB 必需成员，1 个未通过严格文本解码。本记录不保存文件名或来源身份。其中 3 项大小限制均为 TXT，它们记录的是流式修复前的扫描结果；后续超过 800 万字符的合成验证只证明替代工程路径，不证明 V5 已执行真实文风学习。
- 随后使用精确修改前指纹调用 `refresh_proposed_selection`，原子刷新同一个研究。研究标识、公开研究标识、120 部成员和提案指纹全部保持不变。结果仍为 `proposed`：120 个 `pending`，selected／studied／invalidated 均为 0，清单哈希仍为空；没有隐式确认、语义运行或公开发布，也没有创建 V6。
- 已重新生成私有诊断投影，精确返回 120 条本地记录。该投影继续留在私有边界，不序列化到本仓库；这份历史投影不是 120 项文学复核门。
- 全部实现与验证结束后，再次以只读 SQLite 查询核对：仍是同一提案指纹和 `proposed` 状态，精确 120 个 `pending`，清单与失效字段均为空，语义／文风完成回执均为 0，也不存在 V6 研究。
- 历史只读来源批次信号现在标为 `EVIDENCE_SCOPE_ROUTED`，不再是文学 `HOLD`。匿名分流包括：1 项声明语言与内容语言不一致；3 项偏短或完整性信号，其中 1 项同时存在连载／范围不确定性；2 项重启／合并拼接信号。语言不一致会缩窄语言专属结论；不完整或连载材料可以支持局部行文／场景证据，但不能支持未经证实的整部作品结论；重启或拼接信号要求使用边界感知窗口或更窄结论。这些路由不披露来源身份，也不会自动排除作品。
- 首轮全量暴露和 120 项文学复核都不是精确确认门。清洁检查只作用于 AI 实际请求的有界窗口：污染窗口会整体拒绝并补位，不会让来源池失败。精确确认仍要求用户绑定所声明的权利与范围、profile、完整 120 部池成员和 proposal fingerprint。重算后的提案哈希仍为 `sha256:87cebcbc251992ba7b5ed19714117c53d688524c1114734bfaf204e58f6d856b`；清单未锁定，未确认、未运行、未发布。

## 确定性验证结果

- `git diff --check -- specs/031-corpus-style-learning` 通过。
- WSL Python 3.14.4 执行 `python3 scripts/docs_quality.py`：0 项错误、21 项警告；警告全部位于本规格目录以外的既有文件。
- WSL Python 3.14.4 执行 `python3 scripts/quillframe_docs_quality.py`：0 项错误。
- 五组双语文件的标题数量逐组一致（`22/19/14/8/8`），两版研究文档也包含完全相同的 32 个一手来源直达网址。
- 第一次在 PowerShell 直接执行 `python scripts/docs_quality.py` 时，因为该 shell 的 `PATH` 中没有 `python` 而无法启动；随后 WSL 成功执行仓库检查。这是环境路径问题，不是文档断言失败。
- WSL Python 3.14.4 在不调用模型的前提下运行完整仓库套件：`python3 -X utf8 -m unittest discover -s tests -p 'test_*.py'`，697.441 秒完成 1,064 项测试，结果为 `OK`。
- Windows 内置 Python 运行 Writer 技法指导套件，24 项通过；运行历史资格记录套件，14 项通过。托管 Corpus operation matrix 与原生契约对齐后，精确回归也通过。
- 流式路径聚焦组通过 47 项单元测试：采样 18 项、语料库 23 项、文风运行器 6 项。历史合成诊断显示，超过 800 万 Unicode 字符的 TXT 在不整体读取来源的情况下完成，跟踪峰值内存低于 24 MiB（Windows 38.7 秒，WSL 32.4 秒）；另一个 790 万字符的全 TXT 路由中，采样耗时 46.445 秒、物化耗时 9.439 秒，跟踪峰值分别为 6.177 MiB 和 5.715 MiB，独立进程 RSS 增量为 18.766 MiB。这些数字只诊断有界 I/O；CPU、耗时和内存阈值都不是语料确认门或文学质量门。
- Windows Node 在 `PYTHONUTF8=1` 下运行完整 Studio app 套件，117 项全部通过；Studio TypeScript 通过 `tsc --noEmit`。重建后的 Cloud TypeScript 输出通过全部 8 项来源链／operation-matrix 测试。
- Python 解析了依赖与构建目录之外的 1,418 个仓库 JSON 文件；WSL PyYAML 成功解析 `HARNESS_MANIFEST.yaml`。选定 Python 包编译通过，语义引用完整性以 41 个登记契约通过，语义路由器自测通过且未执行模型。
- 在仓库外构建并安装干净 wheel；安装包包含全部 Style Atlas、发布回执、迁移回执、信任策略和登记资源，默认空登记与 `unconfigured` 信任策略均验证通过。wheel 的 SHA-256 为 `b21143c8cb966bcf951fc9fbabf95fce35cf4619425286561e1e3816b4358680`。
- Windows 内置 Python 的直接全量尝试不作为权威结论，因为该运行时缺少仓库要求的原生 no-follow 能力，且其 shell 不能发现 PyYAML／Node。相同仓库套件已在 WSL 完整通过，Windows 专属聚焦与前端套件也在各自支持的宿主环境中通过。

这些检查建立确定性工程与打包集成证据，但不改变下方任何语义、文学或发布状态。

## 集成工程结果

- WSL Python 3.14.4 执行 `python3 -m unittest tests.test_quillframe_style_contract`：21 项测试通过。
- 所有可寻址的 TXT——包括低于旧内存阈值的作品——现在都只经过一次规范化分块流，单次原始读取为 256 KiB。公开采样器把单块限制在 262,144 个 Unicode 字符和 1 MiB UTF-8，把单来源总量限制在 3,200 万 Unicode 字符和 128 MiB UTF-8。解码流只在当次调用中进入匿名 UTF-32 临时文件，临时 SQLite 只保存偏移、预计算检索提示和哈希，不保存文学分值。已选有界片段进入同轮临时缓存，正常语义调用不再重复扫描作品；只有崩溃／新进程恢复才按精确指纹重新物化。超限 EPUB 仍以类型化错误关闭失败；这只证明有界物化，不是行文学习方法，也不要求暴露整个来源池。
- 研究启动、单次扫描采样、崩溃／恢复重物化和发布依赖复核都通过流式方式计算原始身份，不整体读取来源字节。绑定包括精确活跃受治理版本及其解析／可用状态，原始文件读取前后的设备号、文件号、大小、修改时间，实际字节数和预期 SHA-256，解码全文 SHA-256、Unicode 字符数和 UTF-8 字节数，上一轮清单中的逐窗口片段指纹，以及最终物化片段的指纹和长度。任一不匹配都关闭失败。
- 窗口清洁门遇到网址／域名、HTML／脚本或分发／导航样板时，会整体拒绝候选窗口，再从其余候选中确定性补位；禁止删掉可疑行后生成另一个片段，物化时也会再次检查。普通身材／外貌描写，包括孤立出现的“巨乳”，既不是窗口污染，也不是成人配置信号。
- Windows 内置 Python 执行 Style Atlas 发布器、完成回执加载器、StyleStudyRunner、Core、Host Bridge 与 Corpus CLI 聚焦套件：64 项测试通过，另有 1 项符号链接测试在 Windows 跳过；独立 CorpusLibrary 套件另有 20 项测试通过。生产加载器只接受完成回执指纹，由该回执反查研究，重新核对最新完成候选和实际来源文件 SHA-256，再结合构造时注入的可信 provenance resolver 生成发布器要求的封闭候选。伪造回执反查、扫描后来源漂移、调用方自报 provenance，以及未绑定／带额外字段的 provenance 回执都会安全拒绝。
- Studio TypeScript 已通过 `tsc --noEmit`；Corpus Bridge／contracts 的 Node 聚焦套件通过 8 项测试。界面只展示供复核的精确预览指纹／token，不提供 Style Atlas 发布操作，也不会自行制造门禁 `PASS`。
- 发布器安全二审已通过针对调用方自建信任根、角色密钥复用、信任策略漂移、伪造／缺失回执、未签名登记回滚、迁移回执篡改和失败提交清理的聚焦攻防测试。发布与迁移回执均为内容寻址；每个登记 revision 绑定一个事件；可信读取从创世状态重放完整事件链。固定同级信任策略刻意保持 `unconfigured`，所以仓库默认不能执行真实发布。
- AI-native 收尾回归通过 138 项 Corpus／文风／Core／Host 测试（Windows 跳过 1 项符号链接测试）、114 项文风契约／写作技法／语义上下文测试，以及全部 117 项 Studio Node 测试；TypeScript、五项 Studio quality 检查和生产构建也都通过。负测证明非 `continue` 轴不能夹带激活请求，未触及池成员保持未分析，可信发布会重算完整有序清单，同时只为已激活 cohort 复读来源字节。

这些结果只验证有界构造、封闭模式、无来源编译／投影、完成回执寻址加载和界面不发布边界。加载器不持有信任秘密，也不执行发布；宿主没有配置受信 provider 时，Core 默认发布继续阻断。它们没有证明 V5 已真实执行语义分析、盲测已完成、真实语料泄漏已通过、文学质量已改善或取得公开发布权限。

## 当前关卡矩阵

| 关卡 | 所需证据 | 当前状态 |
|---|---|---|
| 研究持久化 | 带采用／改造／拒绝决策的双语一手来源记录 | **RECORDED** |
| V5 实时状态 | 读取当前状态；修改前证明同一标识 V5 未确认、未执行 | **RECORDED** |
| 同标识提案刷新 | 备份、修改前后指纹、原子刷新回执、不建 V6、不隐式确认／执行 | **PASS** |
| 来源池 | 受治理身份／版本账本；精确 120 部成员作为可寻址池 | **ROUTED；等待精确权利／范围／PROFILE／成员／指纹确认** |
| 类型化文风 API | 精确模式构造、验证、指纹和拒绝测试 | **PASS** |
| 有界证据适配器 | 身份绑定、有界物化、清洁、重放和回执 | **有界 TXT 已通过；超限 EPUB 关闭失败** |
| 动态来源池调度 | AI 根据缺口选择下一份合格作品／场景功能证据，并在跨作品收敛时可早于 120 部停止 | **PASS（仅合成契约／运行器）；真实 V5 未运行** |
| 语义综合 | 绑定模型／协议回执、跨作品支持、对照和反例复核 | **NOT RUN** |
| 无来源编译 | 字段移除、封闭模式、合成示例和公开投影测试 | **PASS；公开发布仍阻断** |
| 泄漏防护 | 针对真实私有证据的精确、近似、身份／内容及独立语义检查 | **NOT RUN** |
| 运行时隔离 | 明确选择模式、冻结选择和负向阶段路由测试 | **PASS** |
| 盲测因果评估 | 与来源无关的留出任务、密封标签、交换顺序和独立判断 | **MECHANISM PASS；LIVE EVIDENCE NOT RUN** |
| 人工文学决策 | 作者／真人对新输出作出明确提升或拒绝决定 | **NOT RUN** |
| 公开发布 | 已验证无来源投影、打包／文档同步、回滚和适用权利复核 | **BLOCKED** |

`PENDING` 表示本记录尚未计入实现或确定性验证；`NOT RUN` 表示所需真实语义或人工执行尚未发生；`BLOCKED` 表示依赖关卡通过以前不得发布，并不表示整个项目已放弃。

## 已完成的确定性验证

已经针对本工作树运行聚焦构造／拒绝套件、完整仓库回退套件、Python 编译、JSON／YAML 解析、两套文档检查、语义契约完整性、路由器自测、Studio／Cloud TypeScript 与 Node 检查、已安装 wheel 资源验证和 `git diff --check`。精确环境与主要数量记录在上方。`StyleStudyRunnerTests.test_reconcile_continue_activates_exact_model_work_and_stops_large_pool_early` 还在合成数据上证明：登记的 `next_evidence_requests` 会激活所请求的合格作品和场景功能提示，未触及成员继续保持 `available_unanalysed`，只有实际使用的来源依赖进入回执路径，并且 21 部来源池能够提前停止。这些结果只完成确定性工程关卡，不能满足真实语义、文学、针对私有证据的泄漏检查或公开发布关卡。

## 仍需完成的语义与文学验证

精确确认 V5 以前，用户必须绑定所声明的权利与范围、profile、完整 120 部成员和精确 proposal fingerprint。语言不一致、不完整／连载以及重启／拼接信号在研究期间继续作为类型化证据范围路由，不是文学通过／失败判断。全池暴露、120 项文学复核以及 CPU、耗时或内存阈值都不是确认门。清洁检查仍会把实际选中的污染窗口整体拒绝并请求补位。

同一标识 V5 经过精确确认并另行获准执行后，真实工作流必须保存自适应覆盖、语义观察、证据范围路由、矛盾、跨作品候选、无来源编译与泄漏判断回执。场景／文风分类、缺口分析、下一证据请求和跨作品收敛必须由 AI 负责，而不是关键词、标点或分值启发式。登记契约与合成运行器证据现已证明运行器能够执行所请求的动态作品队列，并在遍历完较大来源池以前停止；它不证明真实模型会作出可靠文学判断，也不表示 V5 已运行。提升仍要使用与来源无关的留出小说任务，对当前对照与候选执行盲法、交换顺序的比较，并完成独立泄漏复核；分别报告内容忠实度、因果运动、目标机制、自然度、可读性、吸引力、多样性、原创性和泄漏，包括平局与回退。

下列情况不是有效证据：

- 只说“处理完 120 部”，却没有场景功能／文风轴覆盖和饱和证据；
- 由知晓实验处理的同一生成路径自我评审；
- 只展示一个精挑开篇或挑选有利输出；
- 把自动指标压成一个提升总分；
- 只做精确匹配，不做近似、内容／专名和语义泄漏复核；
- 把未盲法的作者偏好冒充盲测结果；
- 契约发生实质变化以后，公开投影继续沿用旧批准。

## 发布边界

完整的已授权作品继续留在本地／私有边界。只有依赖、泄漏、评测、文档和人工发布关卡全部通过后，精确封闭、无来源的投影才能进入 Git。非商业目的和抽象处理都不能替代来源链治理或适用权利复核；泄漏检查通过只是技术风险控制结果，不是法律意见。
