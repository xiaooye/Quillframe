# Corpus style learning · Implementation checklist

2026-08-29. Checked items cover persisted research, completed deterministic implementation, or the exact same-ID V5 reconciliation recorded in `verification.en.md`. Unchecked items require a real semantic run, human evidence, commit/release action or applicable rights review; the 120-work cohort does not make any of them complete by itself.

Current checkpoint: V5 remains `proposed` at `sha256:87cebcbc251992ba7b5ed19714117c53d688524c1114734bfaf204e58f6d856b`, with 120 `pending`, an unlocked checklist, no semantic/style run, no completion receipt and no publication.

## Research and decision record

- [x] Read the repository governance, `SYSTEM-IMPROVE`, corpus policy, adaptive-learning protocol and bilingual documentation rules.
- [x] Persist primary-source research on prose craft, serial-platform guidance, interpretable style representation, personalization/adaptation, memorization/leakage and long-form evaluation.
- [x] Record an explicit adopt/adapt/reject disposition for each researched approach.
- [x] Preserve `STUDY-GENERAL-QUALITY-REBUILD-V5`; record that this correction creates no V6 and does not invalidate V5.
- [x] Record that 120 is only the governed source cohort, never a proxy for style-learning depth.
- [x] Record body/appearance description—including the isolated term `巨乳`—as legitimate `general` analysis unless actual context establishes a separately governed explicit-content zone.
- [x] Define the scene-aware adaptive-sampling, typed `StyleContract`, source-free craft-pack, Writer-safe public projection and blind A/B target design.

## V5 state and private boundary

- [x] Re-read the live V5 state and prove that it remains unconfirmed and unrun before mutation.
- [x] Freeze a recoverable private before-state backup, exact proposal fingerprint and dependency inventory.
- [x] Refresh only the same-ID V5 proposal atomically; do not create V6, confirm it, execute it or release it as a side effect.
- [x] Keep complete works, source identities, reversible evidence mappings, private reviewer notes and source-derived passages local/private and out of Git.
- [x] Split alternate editions, translations and serial snapshots by logical work family before any synthesis or held-out allocation.
- [x] Record only anonymous evidence-scope routes: one language-scope signal, three short/completeness-scope signals and two restart/concatenation-boundary signals. They are not a literary-quality `HOLD` and disclose no source identity or reconstructive summary.
- [x] Limit window hygiene to reject/replace behavior for passages actually requested by AI; no pre-confirmation full-window exposure is required.
- [ ] Obtain user confirmation of the exact 120-work available pool's analysis rights, research scope, profile and proposal fingerprint; membership checks do not represent per-item literary review.

## Typed style domain

- [x] Implement and freeze the ten-axis canonical `STYLE_AXES` order.
- [x] Implement and test `quillframe_style_observation_v1` through `make_observation` and `validate_observation`.
- [x] Implement and test `quillframe_cross_work_craft_candidate_v1` through `make_craft_candidate` and `validate_craft_candidate`.
- [x] Require at least two distinct supporting works and at least one counterexample before cross-work compilation, without treating the counts as semantic proof.
- [x] Implement `compile_source_free_craft_candidate`, `compile_style_contract` and `validate_style_contract` for exact `quillframe_style_contract_v1` objects.
- [x] Implement `compile_writer_safe_projection` and `validate_writer_projection` for exact `quillframe_writer_style_projection_v1` objects with no evidence references, record/work/contract/craft IDs or support/counterexample counts.
- [x] Implement `check_local_leakage` and `validate_leakage_report` for exact `quillframe_style_leakage_report_v1`: local `pass|blocked`, always-false `release_ready`, no returned source prose, typed invalid-input failures and an external semantic check that remains unperformed locally.
- [x] Add persistence, fingerprint, schema-rejection, stale-dependency and invalidation receipts without coupling the new state to Canon or production run state.

These components have focused construction, rejection and persistence tests. Final integrated results are recorded separately and do not establish semantic convergence, literary improvement or release authority.

## Scene-aware learning

- [x] Build an addressable range inventory that owns only mechanical boundaries, locators and fingerprints, not source prose.
- [x] Remove keyword/punctuation scoring authority over scene function and style class: deterministic signals are retrieval hints only, while the model contract returns the observed scene functions, evidence scope and uncertainty.
- [x] Provide bound semantic contracts for per-work observation and synthesis, including coverage gaps and local saturation state.
- [x] Let the model contract rather than an ordinal queue choose the next eligible work and scene-function evidence request; synthetic runner tests prove auditable dynamic activation while untouched members remain available and unanalysed.
- [x] Let the model contract propose early convergence from cross-work evidence without requiring all 120 works; registered-contract and synthetic-runner tests prove the engineering path, not a real V5 or literary result.
- [x] Preserve typed states for budget exhaustion, rights blocks and unresolved contradictions without treating them as literary conclusions.
- [x] Stream and normalize every TXT source with bounded reads, including sources below the former in-memory threshold; keep selected windows only in a same-round ephemeral cache, and re-materialize exact fingerprinted spans only after a crash or new-process resume rather than rescanning once per semantic call.
- [x] Bind exact governed/raw source state to decoded-full-text, prior-manifest and final-passage fingerprints; reject drift during sampling, replay and materialization.
- [x] Reject URL/domain, HTML/script and distribution/navigation boilerplate as a whole window and refill deterministically; do not treat ordinary body/appearance language or isolated `巨乳` as contamination or an adult-profile signal.
- [x] Add fixtures for dialogue, action, interiority, exposition, transitions, environment, relationships, body/appearance, openings, middles and closures.
- [x] Bind every semantic observation to protocol, source-version, segment and model-execution provenance; reject quotations, close paraphrases and source-specific narrative detail.

## Source-free compilation and public projection

- [x] Compile only conditions, operations, effects and failure boundaries that survive cross-work contrast and counterexample review.
- [x] Keep the version-1 contract example-free; any future illustration must use an unrelated synthetic premise, never a source-scene rewrite.
- [ ] Run exact token/n-gram, approximate character/MinHash, identity/entity/content and external semantic leakage checks against the private boundary.
- [x] Prove that the public/Writer projection contains only the exact safe schema and sets `attribution_mode=source_free` and `semantic_leakage_check=required_external`.
- [x] Keep aggregate evidence counts inside the internal contract only; keep source/evidence references inside private records only.
- [x] Enforce that no projection can be published until leakage policy, content-zone separation, rollback binding and explicit human release authority all pass; the committed default trust policy remains unconfigured and release stays blocked.

## Runtime isolation and evaluation

- [x] Add an explicit opt-in candidate mode without changing the existing default behavior.
- [x] Select zero to four cards by scene need; validate selection against the frozen contract and preserve current user/Project authority precedence.
- [x] Route the safe projection only to raw-draft and surface-realization Writer stages.
- [x] Add negative tests proving the projection and treatment identity are absent from Blind Reader, independent review, Canon/state consumers and hidden expected data.
- [x] Freeze source-independent, work-family-held-out fiction tasks spanning relevant scene-function × style-axis cells.
- [x] Build sealed, order-swapped blind comparisons among baseline, current craft v4 and the corpus candidate on the same inputs.
- [x] Report content fidelity, causal movement, target mechanism, naturalness, readability, engagement, diversity, originality and leakage separately; do not auto-promote from one weighted score.
- [ ] Collect independent semantic judgments and explicit author/human decisions; invalidate them if the candidate, protocol, treatment or source dependency changes materially.

## Verification, documentation and release

- [x] Run focused unit/integration tests, full deterministic regression tests, schema/fingerprint fixtures, Python compilation, JSON/YAML parsing, documentation QA and `git diff --check`.
- [x] Preserve the historical streaming security/regression and large-file CPU/memory results as adapter diagnostics only; they are not V5 confirmation, semantic-convergence, literary-quality or release gates and do not require a full-pool performance smoke.
- [x] Verify installed-package behavior and manifest/package-file coverage for every intended public artifact.
- [ ] Execute the real V5 semantic workflow only after exact confirmation and authorization; preserve run receipts and failure states.
- [ ] Complete held-out blind literary evaluation and record human promotion/rejection evidence.
- [x] Synchronize the corpus policy, adaptive-learning documentation, semantic-contract catalog, registry/runtime docs, release notes and bilingual mirrors after implementation stabilizes.
- [ ] Commit only source-free, validated public artifacts; never commit the complete local works.
- [ ] Treat leakage checks as risk controls, not copyright clearance, and obtain the applicable release review before calling the public library releasable.
