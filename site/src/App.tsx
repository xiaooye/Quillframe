// Legacy Product Entry implementation removed.
// The single runtime root now lives in ProductApp.tsx so navigation, locale,
// appearance, command palette, and surface chrome have one owner.
export {};
